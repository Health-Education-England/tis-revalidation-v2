events { }

http {
server {
    listen 80;
    server_name _;
    index index.html;
    root /usr/share/nginx/html;
    include /etc/nginx/mime.types;
    location / {
            try_files $uri $uri/ /index.html;
        }
    }
}
